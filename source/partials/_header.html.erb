<header>
  <div class="container">
    <span> Matei Curtasu </span>

    <ul class="navigation">
      <% data.projects.each do |k, h| %>
        <li id="<%= h.id %>"> <%= h.company %> - <i><%= h.project %></i></li>
      <% end %>
    </ul>
  </div>
</header>
